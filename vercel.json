{
  "build": {
    "env": {
      "PIP_OPTIONS": "--no-deps --only-binary :all:"
    }
  },
  "builds": [
    {"src": "/app/main.py", "use": "@vercel/python"}
  ],
  "routes": [
    {"src": "/(.*)", "dest": "app/main.py"}
  ]
}